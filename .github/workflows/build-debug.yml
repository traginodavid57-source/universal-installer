name: Build and Release

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-enabled: true
          cache-read-only: ${{ github.event_name == 'pull_request' }}

      - name: Decode Keystore and Setup key.properties
        run: |
          mkdir -p key
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > key/chave-do-app.jks
          printf "storeFile=../key/chave-do-app.jks\nstorePassword=${{ secrets.KEYSTORE_PASSWORD }}\nkeyAlias=${{ secrets.KEY_ALIAS }}\nkeyPassword=${{ secrets.KEY_PASSWORD }}\n" > key.properties

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Gradle Build Release
        run: ./gradlew :app:assembleOpensourceRelease :tv:assembleRelease

      - name: Upload Release APKs
        uses: actions/upload-artifact@v4
        with:
          name: release-apks
          path: |
            app/build/outputs/apk/opensource/release/*.apk
            tv/build/outputs/apk/release/*.apk
          retention-days: 14
